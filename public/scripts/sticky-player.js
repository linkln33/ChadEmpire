// Script to initialize the music player
document.addEventListener('DOMContentLoaded', function() {
  // The player is now fixed position by default
  // No need for scroll handling
  console.log('Music player initialized');
});
